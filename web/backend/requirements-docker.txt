# ─────────────────────────────────────────────────────────────
# Laguitos Web — Requirements para imagen Docker
# ─────────────────────────────────────────────────────────────
# Usado por web/backend/Dockerfile.
# Superset de web/backend/requirements.txt + yt-dlp (que vive en
# el requirements.txt de la raíz, usado por la desktop app).
#
# NO incluye: pyinstaller, pillow, cairosvg (deps del desktop).
# ─────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Validación y configuración
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# ORM y drivers de BD
sqlalchemy[asyncio]==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1

# HTTP extras
python-multipart==0.0.19
websockets==14.1

# Logging estructurado
python-json-logger==2.0.7

# Core del downloader (compartido con desktop app)
yt-dlp==2026.3.17
